# Kismet capture logs — real captured network data (and typically >100 MB)
*.kismet
*.kismetdb
# ...but do ship the synthetic demo capture
!demo.kismet

# Generated visualisations — may embed real captured device MACs / SSIDs
scope.html
kismet_graph.html

# Rendered demo videos — hosted as GitHub Release assets, not in the repo
*.mp4

# Python
__pycache__/
*.pyc

# Local editor / tooling config
.claude/
